# Historical Codebase Concerns Note

Status: historical planning artifact.

This file is not the current technical-debt source of truth. It previously
tracked early risks such as a monolithic `run` script, old coverage thresholds,
version drift, and legacy path cleanup. Some of those concerns have been
resolved, superseded, or moved into scoped vBRIEFs and issue records.

Current references:

- `vbrief/PROJECT-DEFINITION.vbrief.json` for the project scope registry.
- `vbrief/{proposed,pending,active,completed,cancelled}/` for active and
  historical work state.
- `docs/ARCHITECTURE.md` for current architecture.
- `docs/code-structure-profile.md` for the current codebase metadata profile.
- `task triage:queue` and `task triage:summary` for current backlog state when
  the cache is initialized.

Treat any old risk list in git history as context only. Do not use it as the
active source for work selection.
